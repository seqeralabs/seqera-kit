## To see the full list of options available run: "tw secrets add"
secrets:
  - name: 'my_secret'                         # required
    workspace: 'my_organization/my_workspace' # required
    value: 'my_secret_value'                  # required
    overwrite: True                           # optional